import 'package:paperfold_list/paperfold_list.dart';

/// The axis along which the [PaperfoldList] widget is laid out.
enum PaperfoldAxis {
  /// The list is laid out from `left` to `right`.
  horizontal,

  /// The list is laid out from `top` to `bottom`.
  vertical,
}
